import { UserResolver } from "./UserResolver";
import { PetResolver } from "./PetResolver";
import { RequestResolver } from "./RequestResolver";

export const resolvers = [UserResolver, PetResolver, RequestResolver];
